---
description: 仓库级结构定义与治理约束（不含技术栈细节）
alwaysApply: true
---

# 仓库结构定义与治理约束（强制）

## 适用范围与目标

- 本规则仅定义仓库级约束：目录结构、模块边界、命名规范、协作与交付要求。
- 本规则不绑定具体前后端技术选型，不替代各子系统专项规则。
- 项目初始化阶段优先保证“结构清晰、边界明确、可持续扩展”。

## Monorepo 基本原则

- 采用单一 Git 仓库统一管理前后端代码与工程治理资产。
- 根目录仅承载仓库治理与聚合能力，不承载具体业务实现。
- 前后端代码必须在物理目录上隔离，禁止混放业务代码。

## 根目录职责边界（强制）

- 根目录可包含：仓库说明文档、统一脚本、CI 配置、聚合构建入口、部署与运维资产、规则文件。
- 根目录禁止承载具体业务功能代码（无论前端或后端）。
- 根级配置仅负责统一约束，不得覆盖子项目内部实现细节。

## 标准目录约定（初始化）

- 仓库初始化默认包含以下目录（可按需扩展）：
  - `sca-skeleton-backend/`：后端聚合根目录
  - `sca-skeleton-frontend/`：前端聚合根目录
  - `deploy/`：部署、环境、运维相关资产
  - `scripts/`：仓库级脚本
  - `.cursor/rules/`：仓库治理规则
- 根目录应保留 `README.md` 作为全仓入口说明。

## 子项目边界与耦合约束

- 前后端子项目可独立构建、独立测试、独立发布，禁止强耦合单链路。
- 跨端协作必须通过契约或接口规范完成，禁止通过目录或构建链路隐式耦合。
- 子项目内部规范由各自规则文件约束；仓库级规则仅定义共性边界与协作契约。

## 规则治理约定

- 仓库级规则放置于根目录 `.cursor/rules/` 并作为默认治理入口。
- 仓库级规则文件命名保持语义化，不强制使用编号前缀。
- 专项规则可按主题与编号组织在子目录中（如 `01-architecture/`）。
- 规则冲突处理优先级：用户最新明确决策 > 专项规则 > 仓库总纲规则 > 默认约定。
- 任何规则变更需说明：变更目的、影响范围、兼容策略。

## 文档与交付约束

- 根 `README.md` 必须说明：仓库结构、子项目职责、启动方式、协作约束。
- 子项目目录建议各自维护独立 `README.md`，避免信息集中到根文档。
- 交付说明需区分仓库级信息与子项目实现细节，避免混写。

## 执行与验证

- 每次结构性调整前需明确：目标、影响目录、迁移策略、回滚路径。
- 每次结构性调整后需验证：目录一致性、脚本可用性、文档同步性。
- 未完成上述验证，不视为仓库初始化完成。
