---
description: 仓库级结构定义与治理约束（不含技术栈细节）
alwaysApply: true
---

# 根仓库规则：结构定义与治理约束（强制）

## 执行级别（强制）

- 本规则为根仓库规则，默认必须执行，不作为建议项使用。
- 如与子工程规则冲突，按“用户最新明确决策 > 根仓库规则 > 子工程规则 > 默认约定”处理。
- 任何偏离根仓库规则的实现，必须在变更前完成审批说明（偏离原因、影响范围、回退方案）。
- 未经确认不得跳过根仓库规则中的“必须/禁止”条款。

## 适用范围与目标

- 本规则仅定义根仓库级约束：目录结构、模块边界、命名规范、协作与交付要求。
- 本规则不绑定具体前后端技术选型，不替代子工程规则。
- 项目初始化阶段优先保证“结构清晰、边界明确、可持续扩展”。
- 当前阶段目标仅为“创建项目结构目录”，不包含业务能力开发与技术实现细节。

## Monorepo 基本原则

- 采用单一 Git 仓库统一管理前后端代码与工程治理资产。
- 根目录仅承载仓库治理与聚合能力，不承载具体业务实现。
- 前后端代码必须在物理目录上隔离，禁止混放业务代码。

## 仓库初始化范围约束（强制）

- 本章节仅适用于仓库初始化类任务（目录、规则、说明文档、基础工程骨架）。
- 仅允许进行目录、规则文档、初始化说明的创建与调整。
- 空目录允许保持为空，不要求创建占位文件。
- 禁止在仓库初始化任务中引入业务代码、接口实现、数据库访问层、页面功能实现。
- 禁止在仓库初始化任务中引入与业务功能直接相关的依赖、配置和运行时逻辑。
- 如确需超出范围，必须先获得用户明确确认后再执行。

## 前后端工程根目录定义（强制）

- 后端工程根目录固定为：`sca-skeleton-backend/`。
- 前端工程根目录固定为：`sca-skeleton-frontend/`。
- 未经用户明确确认，不得变更上述根目录名称、层级和职责边界。

## 标准目录约定（初始化）

- 仓库初始化默认包含以下目录（可按需扩展）：
  - `sca-skeleton-backend/`：后端聚合根目录
  - `sca-skeleton-frontend/`：前端聚合根目录
  - `deploy/`：部署、环境、运维相关资产
  - `scripts/`：仓库级脚本
  - `.cursor/rules/`：根仓库规则
- 根目录应保留 `README.md` 作为全仓入口说明。
- 根目录应保留 `.gitignore` 用于统一忽略策略。
- 每个子工程根目录应保留独立 `README.md`。
- 后端工程根目录应保留 `lombok.config` 与 `.editorconfig`。

## 仓库整体目录结构（初始化基线）

- 目录结构参考如下：
  - `sca-skeleton/`
  - `├── pom.xml`
  - `├── .gitignore`
  - `├── README.md`
  - `├── sca-skeleton-backend/`
  - `│   ├── README.md`
  - `│   ├── lombok.config`
  - `│   ├── .editorconfig`
  - `│   ├── pom.xml`
  - `│   ├── sca-skeleton-auth/`
  - `│   │   └── pom.xml`
  - `│   ├── sca-skeleton-gateway/`
  - `│   │   └── pom.xml`
  - `│   └── sca-skeleton-system/`
  - `│       └── pom.xml`
  - `├── sca-skeleton-frontend/`
  - `│   ├── README.md`
  - `│   ├── pom.xml`
  - `│   ├── package.json`
  - `│   ├── turbo.json`
  - `│   ├── pnpm-workspace.yaml`
  - `│   ├── apps/`
  - `│   └── packages/`
  - `├── deploy/`
  - `├── scripts/`
  - `└── .cursor/`
  - `    └── rules/`
- 上述结构为根仓库级约束基线，允许按专题新增子目录，但不得破坏前后端主边界。

## 根目录职责边界（强制）

- 根目录可包含：仓库说明文档、统一脚本、CI 配置、聚合构建入口、部署与运维资产、规则文件。
- 根目录禁止承载具体业务功能代码（无论前端或后端）。
- 根级配置仅负责统一约束，不得覆盖子项目内部实现细节。

## 子工程边界与耦合约束

- 前后端子工程应保持物理隔离与职责隔离，禁止强耦合单链路。
- 跨端协作应通过明确契约或接口规范完成，禁止通过目录或构建链路隐式耦合。
- 子工程内部规范由各自子工程规则约束；根仓库规则仅定义共性边界与协作契约。

## 子工程规则目录与边界（强制）

- 后端工程规则目录固定为：`sca-skeleton-backend/.cursor/rules/`。
- 前端工程规则目录固定为：`sca-skeleton-frontend/.cursor/rules/`。
- 后端规则仅可约束后端工程范围内内容，不得越过 `sca-skeleton-backend/` 边界。
- 前端规则仅可约束前端工程范围内内容，不得越过 `sca-skeleton-frontend/` 边界。
- 子工程规则必须在根仓库规则范围内执行，只能细化，不得放宽、覆盖或绕过根仓库规则。

## 规则治理约定

- 根仓库规则放置于根目录 `.cursor/rules/` 并作为默认治理入口。
- 根仓库规则文件命名保持语义化，不强制使用编号前缀。
- 子工程规则可按主题与编号组织在子目录中（如 `01-architecture/`）。
- 规则冲突处理优先级：用户最新明确决策 > 根仓库规则 > 子工程规则 > 默认约定。
- 任一规则均不得超越根目录 `.cursor/rules/` 下根仓库规则边界；根仓库规则定义通用与上位约束。
- 前后端工程中的子工程规则负责技术与业务细化，与根仓库规则并行不冲突，但必须从属于根仓库规则。
- 任何规则变更需说明：变更目的、影响范围、兼容策略。

## 文档与交付约束

- 根 `README.md` 必须说明：仓库结构、子工程职责、初始化方式、协作约束。
- 子工程目录建议各自维护独立 `README.md`，避免信息集中到根文档。
- 交付说明需区分根仓库级信息与子工程实现细节，避免混写。

## 执行与验证

- 每次结构性调整前需明确：目标、影响目录、迁移策略、回滚路径。
- 每次结构性调整后需验证：目录一致性、空目录保留策略一致性、文档同步性。
- 未完成上述验证，不视为仓库初始化完成。
- 违反本规则的变更不应合入主干，需先完成整改或取得明确豁免。
