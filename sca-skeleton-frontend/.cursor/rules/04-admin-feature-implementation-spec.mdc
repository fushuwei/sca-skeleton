# Admin 功能实现规则（04）

你是资深前端架构师，在 `apps/admin` 中实现后台**功能行为**（数据流、状态、接口与业务逻辑）。本规则与 **「03」主页面布局规则**分工如下：

- **「03」**：壳层分区、尺寸、视觉、通用交互骨架（Header / Sidebar / Tab / 右侧抽屉 / Footer 等「长什么样、怎么摆」）。
- **「04」**：各分区背后的「做什么」——数据来源、用户操作语义、状态持久化、与后端/API 的契约、错误与空态、与工作台及动态路由的联动等。

实现功能时须**同时满足「03」的布局约束**；若纯逻辑改动不涉及视觉，仍以「03」为壳层不变量，避免在壳层偷偷改尺寸或组件选型。

---

## 1）横切能力（全局）

以下能力为多分区共用，优先在本节约定，各分区仅引用要点。

- **鉴权与会话**
  - **实现要点**：登录态存储位置（如 token / cookie）、刷新策略、登出清理范围；路由守卫与接口拦截器的分工（何处跳转登录、何处统一错误码）。
  - **与壳层**：个人下拉「退出登录」须与会话失效一致；不要在多处重复实现登出副作用。
- **菜单与动态路由**
  - **实现要点**：菜单树来源（后端字段含义、`apis/mock` 与真实接口切换约定）；`component` 叶子如何映射到懒加载模块；`ModuleWorkbench` 等静态路由与菜单树的合并规则（对齐 **`routes.ts`**、`utils/menu-tree.ts`、`AdminLayout` 内排序/过滤）。
  - **与壳层**：侧栏选中、`meta.icon` / `meta.title`、Tab 文案与图标须同源规则，避免手写分叉。
- **多 Tab 与路由同步**
  - **实现要点**：`visitedTabs` 的写入时机（首次进入、重复进入同一 path、`query` 变化是否新开 Tab 等）；关闭 Tab 时 Pinia/页面缓存清理策略；**`keep-alive`** 下页面实例生命周期与列表刷新约定。
  - **与壳层**：须遵守「03」中工作台固定首位、不可关闭、`normalizeVisitedTabs` 等约束。
- **右侧抽屉内容契约**
  - **实现要点**：`openRightDrawer(title, icon)` 打开的是「容器」；具体业务应以插槽/子路由/异步面板组件等形式注入，约定标题与图标由调用方传入且与用户心智一致。
  - **与壳层**：固定/浮动、`rightDrawerWidth`、点击主区收起等行为以「03」为准；业务不得在 overlay 模式下阻断壳层规定的收起逻辑（除非产品明确要求并在本规则中增补条目）。
- **国际化（vue-i18n + Quasar Lang）**
  - **语言枚举**：**`en-US`**（English (US)）、**`zh-CN`**（简体中文）；Header 菜单文案固定为上述两条（国旗 **🇺🇸 / 🇨🇳** 为 Unicode emoji 展示列）。
  - **持久化**：**`localStorage`** 键 **`admin-locale`**；合法值仅 **`en-US`** | **`zh-CN`**；缺省或非法时回退 **`zh-CN`**（与首次访问默认中文壳层一致）。
  - **运行时**：切换语言时须同时 **`vue-i18n` `locale`** 与 **`$q.lang.set(...)`**（**`quasar/lang/en-US`** / **`quasar/lang/zh-CN`**），保证 Quasar 组件内置文案与 `<html lang>` 同步。
  - **工程**：**`apps/admin/src/i18n/index.ts`** 暴露 **`getInitialLocale`、`createAdminI18n`、`quasarLangForLocale`、`persistLocale`**；**`main.ts`** 在 **`app.use(Quasar)`** 时 **`lang:`** 与 **`createAdminI18n()`** 初始 **`locale`** 一致；文案位于 **`src/i18n/locales/*.json`**（**`layout.*`** 壳层文案、**`menu.*`** 与路由 **`name`** 对齐的菜单/Tab 标题键）。
  - **壳层生效范围**：**`AdminLayout`** 模板字符串须 **`t('layout.*')`**；侧栏/树菜单用 **`menu.${菜单节点 name}`**（**`te` 命中优先**，否则回退后端 **`title`**）；**`visitedTabs`** 在 **`locale`** 变更时用 **`router.resolve(tab.path)`** + **`tabTitleFromRoute`** 重算标题。
  - **与壳层**：国际化为 **Header 内 `q-menu` 下拉**，**不**走 **`openRightDrawer`**；锚点/offset 遵守「03」Header 专条；语言项单行高度 **`40px`**（**`top-locale-menu-item`**）。
- **亮/暗主题（Quasar Dark）**
  - **默认**：**Light**（**`$q.dark.isActive === false`**）；**`main.ts`** **`Quasar` `config.dark`** 读存储初始化。
  - **持久化**：**`localStorage`** 键 **`admin-theme-dark`**，字符串 **`"true"`** / **`"false"`**；**缺省键**视为 **Light**。
  - **交互**：Header 单按钮 **`$q.dark.toggle()`** 后 **`persistDark($q.dark.isActive)`**；**图标表示点击后将进入的模式**：**明亮态显示 `sym_r_dark_mode`**，**黑暗态显示 `sym_r_light_mode`**（与「03」一致）。
  - **深色适配**：除 **`$q.dark`** 外，须加载 **`apps/admin/src/styles/admin-layout-dark.scss`**（在 **`main.ts`** 于壳层样式之后引入），对 **`.admin-shell`** 内硬编码浅色（**顶栏 `.top-header`**、侧栏、Tab、底栏、主区、抽屉等）及 **工作台 / 占位页** 做 **`body--dark`** 覆盖（顶栏 scoped 为 **`#009688`** 时须在深色表中用 **`!important` 或更高特异度**压过）；teleport 菜单使用 **`content-class` / `class`**（如 **`admin-user-q-menu`**）以便深色下命中样式。
  - **与壳层**：主题按钮为圆形 **`top-action-btn`**，遵守「02」Material Symbols 与 Header state layer；**不**打开右侧抽屉。
- **全局搜索 / 通知 / 回收站（若从占位升级为业务）**
  - **实现要点**：各模块的 API、分页、防抖、权限过滤；从搜索结果跳转路由时是否新开 Tab、如何带回上下文。
  - **与壳层**：默认经 Header 图标打开右侧抽屉展示面板（当前代码多为占位标题，升级时在「04」逐条补齐交互稿级描述）。

---

## 2）顶部 Header（功能语义）

在「03」已定义的图标顺序与样式前提下，逐个定义**点击后的业务行为**（抽屉内容、路由跳转或全局弹层）。

- **Logo / 系统名**：一般为静态展示；若点击回到工作台或门户，在此写明路由目标。
- **全局搜索**：搜索范围（菜单 / 页面 / 数据资产等）、快捷键（若有）、空态与加载态。
- **消息通知**：列表维度、已读/未读、轮询或 WebSocket（若采用则写明重连策略概要）。
- **全局回收站**：恢复与彻底删除的权限与二次确认；与后端资源类型枚举对齐。
- **国际化**：仅 **en-US / zh-CN**；下拉选择后立即 **`locale` + `$q.lang.set`** 并 **`persistLocale`**；业务页面文案是否全部走 **`vue-i18n`** 由迭代逐步替换（壳层菜单项可先仍为后端/静态中文）。
- **亮/暗主题**：**`admin-theme-dark`** + **`$q.dark.toggle`**；详见「1）横切」专条。
- **系统设置**：设置项分组、是否与用户偏好后端同步。
- **用户胶囊与下拉**：真实姓名/邮箱数据来源；「个人中心」「修改密码」「退出登录」各自路由或弹窗流程。

---

## 3）左侧导航面板（功能语义）

- **菜单数据**：与鉴权接口字段一一对应；隐藏工作台、模块排序、「系统管理」居末等过滤规则须在代码中与 **`DEMO_MENUS`** / 后端约定保持一致。
- **手风琴展开/折叠**：是否与用户偏好持久化（localStorage / 后端）；默认「进入主界面全部收缩」是否允许被记忆覆盖。
- **侧栏搜索**：过滤算法（前缀 / 模糊 / 拼音）；高亮与键盘导航；与「03」中 WebKit 聚焦/关闭约定兼容。
- **树节点选择**：点击叶子跳转路由；中间态（权限变更、菜单刷新）下如何重建选中与 Tab。
- **宽度拖拽**：`LEFT_DRAWER_WIDTH_*` 是否持久化；若持久化，写明存储键与默认值回退。

---

## 4）中间主区域（页面功能）

- **工作台 `/dashboard`**：卡片/图表数据来源、刷新频率、空权限时的降级展示。
- **业务页面**：按模块在后续小节或独立文档中罗列（数据源中心、数据资产、数据开发、系统管理等）；每个页面至少约定：主列表/表单依赖的 API、关键查询参数、操作按钮权限编码。
- **缓存策略**：哪些路由纳入 `keep-alive`、命名是否与路由 `name` 一致；离开页是否重置筛选条件（明确产品偏好）。

---

## 5）右侧抽屉（按入口拆分）

对每个 `openRightDrawer(...)` 调用建立一条「面板规格」：

- **面板标识**：建议稳定 `panelKey`（便于埋点与缓存）。
- **内容组件**：路径与异步加载方式。
- **数据加载**：进入时拉取、依赖参数（当前用户、当前路由上下文等）。
- **关闭与脏数据**：表单未保存时是否拦截关闭。
- **固定侧栏模式**：面板内交互是否依赖「主区宽度变化」（必要时注明最小宽度下的降级）。

当前仍适合 **`openRightDrawer`** 占位或落地的入口：**全局搜索、消息通知、全局回收站、系统设置**；用户菜单链路的 **修改密码**；Footer 的 **Database**、**History**。**国际化、亮/暗主题**已改为 Header **内联菜单 / 单键切换**（见「1）」「2）」及「03」），**不再**占用右侧抽屉占位。

---

## 6）底部状态栏（功能语义）

- **左侧**：切换 `leftDrawerOpen` 仅联动布局；若需记忆展开状态，写明策略。
- **中间**：版本号来源（构建注入 `import.meta.env` / 后端 `/version`）；若走后端，写明轮询与否。
- **右侧 Database / History**：与 Header 同源抽屉契约；分别定义面板能力与权限。

---

## 实现约束（汇总）

- **边界**：不在本规则中重复「03」「02」已写的尺寸、图标集、圆角例外、overscroll 等壳层与视觉细则；冲突时以编号更新的规则为准。
- **最小可行**：优先贯通一条完整链路（接口 → 状态 → UI），避免只有 mock 无契约或只有 UI 无数据来源。
- **可验证**：每一项功能在 PR 或自检清单中应能对上「触发入口 → 期望数据/跳转 → 异常表现」三步描述。
- **演进方式**：新增分区或大功能时，在本文件对应章节追加小节，保持与「03」分区编号对照，便于检索。
