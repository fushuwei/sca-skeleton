---
description: 后端安全架构与三角色职责边界（auth / gateway / resource server）
alwaysApply: true
---

# 后端安全架构与三角色职责边界（强制）

## 适用范围（强制）

- 本规则适用于 `sca-skeleton-backend/` 内所有承担鉴权、令牌校验、用户上下文传递职责的模块。
- 与根仓库规则、03-OAuth2 认证规则冲突时按既定优先级处理。
- 本规则定义的“三角色”是认证体系职责的最小切分，禁止合并、跨角色实现或新增并行角色。

## 三角色定义（强制）

- 认证中心（Authorization Server）：
  - 唯一归属模块：`sca-skeleton-auth`
  - 核心职责：账号体系访问、密码校验、客户端认证、令牌颁发、标准 HTTP 自省/吊销端点、OIDC 元数据、Redis 授权持久化
  - 必须独立部署，不得与业务服务共进程
- 网关（API Gateway）：
  - 唯一归属模块：`sca-skeleton-gateway`
  - 核心职责：南北向流量入口、白名单放行、Bearer 存在性前置检查（非白名单无 token 则 401）、外部伪造头清理、TraceId 透传、限流与基础防护
  - **禁止**：OAuth2 自省、Redis 令牌校验、用户上下文头注入、403 权限判定
  - 严格遵循“透明转发”原则，业务编排禁止下沉到网关
- 资源服务器（Resource Server）：
  - 归属模块：所有业务微服务（如 `sca-skeleton-system` 及未来扩展模块）
  - 核心职责：经 Redis 本地自省校验 access_token、从 SecurityContext 读取用户上下文、执行方法级权限控制、提供业务接口
  - 必须通过 `sca-skeleton-starter-security` 统一接入，禁止逐模块重复造轮

## 链路与调用方向（强制）

- 标准认证链路：客户端 → 网关（白名单 / Bearer 存在性）→ 业务服务（Redis 自省 + `@RequiresPermission`）→ 业务逻辑。
- 令牌颁发链路：客户端 → 网关 → 认证中心 `/oauth2/token`（网关对该路径做白名单放行，仅透传不做语义改写）。
- 内部微服务 **禁止** 以 HTTP 调用 `/oauth2/introspect` 作为默认主链路；HTTP 自省端点保留给外部集成与合规场景。
- 服务间内部调用必须携带服务身份（如 `client_credentials` 颁发的 access_token 或 mTLS），禁止依赖网关注入的用户头作为唯一身份依据。

## 网关侧硬约束（强制）

- 必须使用响应式技术栈：`spring-cloud-starter-gateway-server-webflux`，**禁止** 引入 `spring-boot-starter-security`；鉴权前置由自定义 `GlobalFilter` 完成，禁止与同进程内 Servlet 资源服务器混用。
- 必须实现外部请求头治理过滤器，清理或重置以下伪造风险头：`X-User-Id`、`X-User-Name`、`X-Tenant-Id`、`X-User-Roles`、`X-From` 等内部头；hop-by-hop 头按 RFC 7230 一并清理。
- **禁止** 在认证通过后向下游写入 `X-User-Id`、`X-User-Name`、`X-Tenant-Id`；用户上下文由下游从 `BearerTokenAuthentication` / `SecurityUtils` 获取。
- **必须** 由网关统一生成 `X-Trace-Id`（不信任外网/浏览器传入值；先剥离客户端头再写入新 ID）。
- 非白名单且缺失/空 Bearer 必须返回 401 + JSON，响应 `WWW-Authenticate: Bearer`；**禁止** 在网关返回 403（权限不足仅在资源服务器返回 403）。
- 白名单仅限：登录端点、登出端点、验证码端点、健康检查、文档端点等无副作用接口，禁止把任何业务读写接口加入白名单。
- 限流 KeyResolver **必须** 优先使用 Bearer token 字符串，无 token 时回退客户端 IP。
- 网关全局响应头必须固定包含安全基线：`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`；CORS 必须显式枚举来源，禁止生产环境使用 `*`。

## 资源服务器硬约束（强制）

- 必须以无状态模式运行（`SessionCreationPolicy.STATELESS`），禁止依赖 HTTP Session。
- 必须显式禁用 CSRF（不透明令牌 + Bearer 头场景）。
- 必须通过 `RedisOpaqueTokenIntrospector` + `PermissionsOpaqueTokenAuthenticationConverter` 把 `permissions` 转换为 `GrantedAuthority`，权限码不带 `ROLE_` 前缀。
- 接口级权限统一通过 `@RequiresPermission("xxx:yyy:zzz")` 或 `@PreAuthorize("hasAuthority('xxx:yyy:zzz')")` 标注，禁止在业务代码中手写 `SecurityContextHolder` 校验。
- 当前用户上下文统一通过 `SecurityUtils` 与 `CurrentUserProvider` 获取，禁止直接读取 `BearerTokenAuthentication` 内部字段（业务层）。
- 资源服务器自身不得签发或刷新令牌；**允许** 通过 starter 只读访问 OAuth2 授权 Redis，**禁止** 直接读写认证中心 JDBC 用户表或篡改授权数据。

## 多租户与权限模型对齐（强制）

- `tenant_id` 必须作为 access_token 自省响应的固定字段，资源服务器在数据访问层必须将 `SecurityUtils.getTenantId()` 作为基础过滤条件。
- RBAC 模型（用户-角色-权限）统一由 `sca-skeleton-system` 维护数据，认证中心仅在颁发令牌时按一次性视图读取最新权限并写入 `permissions` claim。
- 跨服务读取权限不允许走 token 之外的旁路；如确需，必须通过 system 服务公开的 API 与服务间令牌完成。

## 与令牌格式的对齐（强制）

- 三角色对令牌格式的认知必须一致：
  - 认证中心：颁发**不透明** access_token（REFERENCE）+ refresh_token，OIDC id_token 为 JWT
  - 网关：**不校验** access_token 有效性，仅检查 Bearer 是否存在
  - 资源服务器：以 **Redis 本地自省** 校验 access_token，不得使用 `JwtDecoder`/`JwtReactiveAuthenticationManager`
- 任何角色单独切换为 JWT 自验签链路都视为破坏一致性，必须提交全局影响评估。

## 失败处理与可观测性（强制）

- 401/403 响应必须包含统一字段：`code`、`message`、`data`（与项目统一响应体语义一致）。
- 网关 401、资源服务器令牌失效 401、资源服务器权限不足 403 三类典型异常必须分别可被监控告警识别。
- 链路追踪必须从网关到资源服务器全链路贯通：网关生成或透传 `X-Trace-Id`，业务服务进入时必须落入 `MDC` 以便日志关联；**禁止** 为在网关日志中打印 userId 而恢复网关注入用户头或网关自省。

## 公共 Starter 复用约束（强制）

- 资源服务器能力必须通过 `sca-skeleton-starter-security` 统一封装：
  - 自动装配 `SecurityFilterChain`、`RedisOpaqueTokenIntrospector`、`PermissionsOpaqueTokenAuthenticationConverter`
  - 暴露统一的 `sca.security.*` 配置项与默认白名单
  - 提供 `SecurityUtils`、`CurrentUserProvider` 与 `@RequiresPermission` 语法糖
- 模块如需扩展能力（如自定义认证转换器、附加业务 claim 字段），必须以 starter 钩子或 `@ConditionalOnMissingBean` 的方式实现，禁止直接在业务模块声明同名 `SecurityFilterChain` 覆盖默认链。

## 变更约束（强制）

- 任何会改变三角色边界、自省链路、上下文头、白名单语义的修改，必须先更新本规则再实施代码改造。
- 偏离本规则的实现必须提交：偏离原因、影响范围、回退方案，并获得明确确认后再执行。
