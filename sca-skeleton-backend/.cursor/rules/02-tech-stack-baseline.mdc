---
description: 后端技术栈与版本治理基线规则
alwaysApply: true
---

# 后端技术栈基线规则（强制）

## 适用范围

- 本规则适用于 `sca-skeleton-backend/` 全部后端模块。
- 与根仓库规则冲突时，以根仓库规则和用户最新明确决策为准。

## 技术栈基线

- 本章节技术栈为强制基线，未经明确批准不得替换同类主技术方案。
- Java：应用运行时与编译基线；统一使用 `JDK 25 LTS`。
- Spring Boot：应用框架与自动配置基线；统一使用“最新可用稳定版”。
- Spring Cloud：微服务治理基线（服务发现、配置整合、调用治理）；统一使用“与 Spring Boot 对齐的最新可用稳定版”。
- Spring Authorization Server：认证中心与令牌签发基线；统一使用“最新可用稳定版”。
- MyBatis-Plus：数据访问与 ORM 增强基线；统一使用“最新可用稳定版”。
- Redis：缓存与高频读写加速基线；统一使用“最新可用稳定版”。
- Redisson：分布式锁与分布式结构化能力基线；统一使用“与 Redis 兼容的最新可用稳定版”。
- Nacos：服务注册与配置中心基线；统一使用“最新可用稳定版”。
- 全局 ID 生成基线统一使用字符串形态 UUID，统一采用去掉连字符（`-`）的 32 位小写格式，禁止使用自增 ID 与雪花 ID。
- UUID 生成实现统一使用 `UuidUtils.nextSimpleStr()`，禁止 `UUID.randomUUID().toString()` 原样入库。

## 版本治理策略

- 依赖版本统一由 `sca-skeleton-dependencies` 管理。
- 后端工程根目录 `pom.xml` 不直接维护业务依赖版本清单，仅负责聚合与构建治理。
- 子模块禁止随意声明与统一 BOM 冲突的依赖版本。
- 禁止引入 `SNAPSHOT`、里程碑版、RC 版本；如确需使用，必须先获得明确确认。
- “最新版本”默认语义为“官方发布的最新稳定 GA 版本”，不含预发布版本。
- 各技术栈具体版本号应在 `sca-skeleton-dependencies` 中显式锁定，禁止在业务子模块分散锁版本。

## 依赖引入准入

- 新增依赖必须满足：与当前模块职责直接相关、收益明确、维护状态可接受。
- 同类能力优先复用现有技术栈，禁止并行引入重复技术方案。
- 任何新增依赖都应说明必要性与影响范围（体积、启动时间、学习成本、安全面）。

## 升级与替换原则

- 版本升级采用小步可回滚策略，单次变更聚焦同一技术域，避免多技术域叠加升级。
- 依赖替换必须先评估兼容性（API、配置、行为语义），并给出迁移方案。
- 已标记废弃的依赖或用法禁止在新代码继续引入。

## 变更验证要求

- 涉及技术栈或版本变更时，必须补充影响模块、兼容性评估和回滚方式。
- 变更后至少完成：全量编译通过、关键模块启动通过、核心链路冒烟验证通过。

## 例外机制

- 任何偏离本规则的实现，必须在变更前完成审批说明并获得明确确认。
