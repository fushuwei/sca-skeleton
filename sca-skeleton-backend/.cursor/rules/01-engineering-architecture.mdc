---
description: 生产级微服务脚手架创建与演进总纲规则
alwaysApply: true
---

# 01 项目脚手架创建与框架总纲（强制）

## 规则体系与编号治理（强制）

- 规则目录按领域分层，当前约定：
  - `01-architecture`: 系统架构、总纲、工程治理、框架边界
  - `02-starters`: starter 边界与依赖治理
  - `03-gateway`: 网关职责、鉴权前置、流量治理
  - `04-auth`: 认证闭环、令牌策略、安全规则
- 新增规则文件命名格式统一为：`<编号>-<主题>.mdc`，编号在所属目录内递增，禁止无编号文件。
- 规则间冲突处理顺序：用户最新明确决策 > 专项规则 > `01-architecture` 总纲 > 默认约定。

## 项目定位与目标（强制）

- 本项目定位为生产可演进的后端微服务基础平台，优先保证稳定性、可维护性、可观测性、可扩展性。
- 项目创建阶段以“最小可运行闭环”优先，不以功能数量作为进度标准。
- 若发现架构、命名、边界不合理，必须先提交“问题清单 + 方案对比 + 影响评估”，经确认后再调整。

## 技术栈基线（强制）

- Java: JDK 25
- 除 JDK 外，核心依赖统一使用“当前最新稳定版”（禁止里程碑版/快照版，除非用户明确批准）。
- Spring Boot / Spring Cloud / Spring Cloud Alibaba / Spring Authorization Server / MyBatis-Plus / Redisson 版本选择遵循上一条。
- 基础设施: Nacos（注册/配置）、Gateway、OAuth2 Authorization Server、Redis/Redisson
- 持久层: MyBatis-Plus
- 工具链: Lombok、MapStruct

## 仓库与模块结构（强制）

- 采用 Maven 多模块聚合，根工程仅负责依赖治理、插件治理、统一构建，不承载业务代码。
- 模块职责最小化且边界清晰，至少包含：
  - `sca-skeleton-gateway`: 流量入口、认证前置、路由与基础防护
  - `sca-skeleton-auth`: 认证与令牌签发（OAuth2 标准链路）
  - `sca-skeleton-system`: 系统基础域能力（用户、角色、权限等）
  - `sca-skeleton-starters`: 跨模块复用能力（core/web/logging/captcha）
  - `deploy`: 部署脚本、配置模板、SQL、运维文档
- 禁止网关承载业务逻辑；禁止认证模块承载系统业务域逻辑；禁止 starter 承载具体业务实现。

## 依赖与版本治理（强制）

- 统一使用父 POM 的 `dependencyManagement` 管理版本，子模块禁止随意声明漂移版本。
- 禁止循环依赖、禁止跨层反向依赖、禁止引入职责无关的大体量依赖。
- starter 之间依赖必须单向且可解释，`core` 可被依赖，`web` 不可反向依赖 `core`。
- 每次新增依赖必须说明必要性与影响面。

## 配置治理与环境约束（强制）

- 微服务模块内 `application.yml` 仅保留最小引导配置，不固化环境私参。
- 环境配置统一维护在 `deploy/nacos/config/<env>/`，通过配置同步流程发布到 Nacos。
- Nacos 配置导入约定统一为：`{parent}-common` + `{application}` + `{application}-{profile}`。
- 禁止通过 SQL 直接变更 Nacos 存储数据；仅允许 OpenAPI 或控制台方式变更。
- `dev` 环境必须可本地直接启动，缺省值需提供本地可运行兜底。

## 认证与安全底线（强制）

- 认证与令牌必须遵循 Spring Authorization Server 标准链路，禁止手写 token 逻辑替代协议主链路。
- 默认按零信任设计接口鉴权与数据访问，不可仅依赖前端传参判断权限。
- 网关负责认证前置与粗粒度拦截，下游服务必须执行资源级二次鉴权。
- 认证闭环细则（验证码、刷新策略、401/403 分流）由 `100-auth-closed-loop.mdc` 约束。

## 全局 ID 规则（强制）

- 全局唯一 ID 一律通过 `UuidUtils` 生成。
- 默认统一使用不带横杠的 UUID：`UuidUtils.nextSimpleStr()`。
- 非经用户明确批准，不得引入自增主键、雪花算法或其他 ID 生成方案。

## 数据模型与 SQL 事实源（强制）

- 数据模型以 `deploy/sql/install/*.sql` 为唯一事实源。
- 代码实现（实体、Mapper、Service、API）必须对齐 SQL 字段语义、命名、约束。
- 若代码与 SQL 冲突，默认修正代码对齐 SQL；仅经确认后才调整 SQL。
- 软删除、乐观锁、审计字段策略必须全局一致。

## 编码规范与文档要求（强制）

- 仅实现当前任务直接需要的最小闭环，不做超范围抽象与无关重构。
- 保持模块内风格一致；新增类和方法需具备清晰注释，核心逻辑需有必要行级注释。
- 每次改动必须可追溯到明确需求，禁止“顺手修改”无关内容。
- 新增配置键使用通用前缀（如 `auth`、`security`、`oauth2`），禁止新增 `sca` / `skeleton` 前缀配置键。

## 过程与交付要求（强制）

- 在执行任务前必须给出：目标、影响模块、风险点、验证方案、回滚方式。
- 任务完成后必须给出：变更清单、验证命令、预期结果、未决风险。
- 涉及数据库功能时必须标注对应 SQL 文件与表名，确保语义一致。
- 规则文件变更默认遵循“先增后改”：新增优先，删除或迁移需确认后实施。

## 项目创建默认里程碑（强制）

- 里程碑 A：工程骨架与依赖治理可用（可编译、可启动基础模块）。
- 里程碑 B：认证中心可用（标准 OAuth2 主链路可发放与校验令牌）。
- 里程碑 C：网关鉴权可用（`401/403` 分流稳定）。
- 里程碑 D：系统服务接入可用（鉴权上下文可透传并可二次鉴权）。
- 里程碑 E：交付可用（部署文档、验证脚本、最小运维手册齐备）。
