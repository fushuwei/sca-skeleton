---
description: 后端工程架构规则（模块边界、包结构与依赖方向）
alwaysApply: true
---

# 后端工程架构规则（强制）

## 适用范围（强制）

- 本规则仅约束 `sca-skeleton-backend/` 内的后端工程结构与架构边界。
- 与根仓库规则冲突时，以根仓库规则和用户最新明确决策为准。

## 模块边界（强制）

- 后端采用 Maven 多模块聚合；聚合根仅负责依赖治理与构建编排，不承载业务实现。
- 模块职责必须单一且清晰，禁止跨模块承载他模块核心职责。
- 网关模块只负责流量接入、鉴权前置、路由与通用防护，禁止承载业务编排与业务规则。
- 认证模块只负责认证与令牌相关能力，禁止承载系统业务域实现。
- 复用模块（如 starter/common）只承载通用能力，禁止落地具体业务逻辑。

## 模块命名规范（强制）

- 模块名统一使用小写短横线风格：`sca-skeleton-<domain>`。
- `<domain>` 必须表达稳定业务域或平台能力，禁止使用临时性、阶段性命名。
- 新增模块前必须先定义模块职责、上游依赖、下游被依赖关系。
- 模块 `artifactId` 必须与模块目录名一致，禁止目录名与 `artifactId` 脱节。
- 基准服务命名固定如下，未经明确确认不得重命名：
  - `sca-skeleton-dependencies`
  - `sca-skeleton-gateway`
  - `sca-skeleton-auth`
  - `sca-skeleton-system`
  - `sca-skeleton-starters`
- 若确需调整基准服务命名，必须先提交影响评估（构建、配置、部署、调用链、文档）并获得确认后实施。

## 包结构规范（强制）

- Java 包名根前缀统一为 **`io.github.fushuwei.scaskeleton`**（全小写连写）。
- 禁止使用的包名形态：
  - `io.github.fushuwei.sca.*`（`sca` 仅用于 Maven `artifactId`/目录名，不作为 Java 包名段）。
  - `io.github.fushuwei.scaskeleton.starter.*`（`starter` 仅用于 Maven 工件命名，不作为 Java 包名段；与 Spring Boot 惯例一致：工件叫 `spring-boot-starter-*`，包名按能力域划分）。
- Maven `groupId` 统一为 `io.github.fushuwei`（与 Java 包名根不同，禁止将 `groupId` 误当作包名前缀）；未确认前禁止新增平行 `groupId`。
- Maven `artifactId` 与 Java 包名对应关系（工件名可含 `starter`，包名按**能力域**命名）：
  - 可部署业务服务：`sca-skeleton-<domain>` → `io.github.fushuwei.scaskeleton.<domain>`（如 `auth`、`system`、`gateway`）。
  - 横切能力库（含平台核心）：`sca-skeleton-starter-<capability>` → `io.github.fushuwei.scaskeleton.<capability>`。示例：`starter-core` → `scaskeleton.core`（`constant`、`exception`、`uuid` 等子包）、`starter-security` → `scaskeleton.security`、`starter-web` → `scaskeleton.web`、`starter-mybatis-plus` → `scaskeleton.mybatis`。其中 `starter-core` 为各 Starter 的公共依赖基础，包名仍遵循同一映射，不单独占一条规则。
- 模块内按经典 MVC 分层组织包结构，固定为：`controller`（控制器层）、`api`（请求/响应对象层）、`service`（业务层）、`entity`（实体层）、`mapper`（持久层）、`converter`（对象转换层）、`config`（配置层）。
- `controller` 包与 `api`、`service`、`entity`、`mapper`、`converter`、`config` 同级，位于模块根包下。
- `api` 层仅包含请求对象（`request`）和响应对象（`response`），按业务域子包组织。
- 控制器仅允许出现在 `controller` 包；请求对象、响应对象仅允许出现在 `api` 层及其子包。
- 实体类仅允许出现在 `entity` 包，Mapper 接口仅允许出现在 `mapper` 包。
- 禁止在包名中使用 `tmp`、`test1`、`new` 等非语义化命名。
- 禁止使用 DDD 分层包名（`application`、`domain`、`infrastructure`），本工程不采用 DDD 架构。

## Bean 转换规范（强制）

- Entity ↔ Response / Request ↔ Entity 的对象转换统一使用 MapStruct，禁止使用 `BeanUtils.copyProperties` 等反射方式。
- MapStruct Converter 接口统一放置在 `converter` 包下，使用 `@Mapper(componentModel = "spring")` 声明为 Spring Bean。
- Converter 接口命名统一为 `XxxConverter`，方法命名遵循 `to<目标类名>` 约定（如 `toUserResponse`、`toUserProfileResponse`、`toDeptResponse`），方法名必须包含目标类型，禁止使用 `toResponse` 等泛化命名。
- Converter 仅负责字段映射，不承载业务逻辑；复杂映射规则通过 `@Mapping` 注解显式声明。
- Service 层通过构造器注入 Converter，直接调用 Converter 方法完成转换（如 `userConverter.toUserResponse(user)`），禁止为单行委托定义额外的私有包装方法。

## 依赖方向约束（强制）

- 必须保持单向依赖：`controller -> service -> entity / mapper`；`controller` 和 `service` 均可引用 `api`（request/response）对象。
- `converter` 依赖 `entity` 和 `api`（response），由 `service` 注入调用。
- `entity` 层禁止依赖 `api`、`service`、`controller` 以及具体框架实现。
- 控制器（`controller`）禁止直接依赖 `entity` 和 `mapper`，只能通过 `service` 间接访问。
- 禁止循环依赖、禁止跨层反向依赖、禁止通过工具类绕过分层边界。
- 跨模块调用优先通过显式契约（接口/DTO），禁止直接依赖对方基础设施实现。

## 配置与版本治理（强制）

- 依赖版本统一由 `sca-skeleton-dependencies` 管理，不在后端工程根目录 `pom.xml` 中直接维护业务依赖版本清单。
- 后端工程根目录 `pom.xml` 仅承担聚合编排、插件治理与构建治理职责，不承载业务依赖版本治理职责。
- 子模块依赖版本应继承 `sca-skeleton-dependencies`（或其上游统一 BOM），不得随意声明漂移版本。
- `sca-skeleton-dependencies` 应保持 `pom` 打包类型，并以 BOM 方式被子模块 `import` 使用。
- 模块内 `application.yml` 应保留生产级标准启动配置基线，环境差异配置不得硬编码到代码仓库私有参数。
- 新增依赖必须与模块职责直接相关，并说明必要性；职责无关依赖禁止引入。

## 全局 ID 与主键策略（强制）

- 全系统唯一 ID（业务 ID、数据库主键、关联外键）统一使用 UUID 体系。
- 默认使用字符串形态 UUID，统一采用去掉连字符（`-`）的 32 位小写格式，禁止引入自增主键方案。
- 统一通过 `UuidUtils.nextSimpleStr()` 生成 ID，禁止 `UUID.randomUUID().toString()` 原样入库。
- 禁止引入雪花算法或其他数字分布式 ID 方案作为主键。

## Spring 配置类规约（强制）

- `sca-skeleton-backend/` 范围内所有 `@Configuration` 类必须显式声明 `@Configuration(proxyBeanMethods = false)`，不得使用默认 `true`。
- `@Configuration` 类内部 `@Bean` 方法之间禁止直接调用同类的其他 `@Bean` 方法；依赖其他 Bean 时必须通过方法参数注入由容器解析单例。
- 调用同类非 `@Bean` 的私有/静态工具方法不在限制范围内。
- 本规约仅约束 `@Configuration` 注解的类，不影响 `@Component`、`@Service`、`@Repository`、`@Controller` 等其他 stereotype 注解的类。
- 基础设施型 Bean（`BeanPostProcessor`、`BeanFactoryPostProcessor` 及与容器早期生命周期相关的 Bean）的 `@Bean` 工厂方法必须声明为 `static`。
- 属性载体类（`@ConfigurationProperties`）禁止叠加 `@Configuration`/`@Component`，必须通过 `@EnableConfigurationProperties` 在配置类中显式启用，避免因短类名冲突造成 Bean 注册失败。
- 例外审批：如确需保留默认 CGLIB 代理增强语义（同类 `@Bean` 方法递归调用等场景），必须在变更前提交偏离说明（场景、影响范围、回退方案）并获得明确确认，并在类注释中标注豁免原因。
- 新增或修改 `@Configuration` 类时，未显式声明 `proxyBeanMethods = false` 视为不符合交付标准，不允许提交。

## 变更约束（强制）

- 仅做与当前需求直接相关且满足生产级质量要求的变更，禁止借机重构无关模块。
- 任何会改变模块边界或包结构的改动，必须先明确影响范围与迁移策略。

## 执行质量约束（强制）

- 对同类问题必须一次性全局处理，禁止只修复单点后继续重复犯错。
- 遇到技术栈主版本与工件名、BOM 管理范围等明显关联项时，必须先校验再落地，不得凭经验假设。
- 若发现规则描述存在歧义、冲突或覆盖不足，必须先暂停实施并向用户确认，不得自行延伸解释。
- 回答与注释风格应自然、直接、可读，禁止模板化重复措辞和生硬术语堆叠。
- 用户指出的风格偏好（如命名风格、注释粒度、表达习惯）属于后续同类场景默认约束，除非用户明确变更。

## 注释规范（强制）

- `sca-skeleton-backend` 范围内所有类必须包含类级注释，且类注释中必须包含 `@author Fu Wei`。
- `sca-skeleton-gateway`、`sca-skeleton-auth`、`sca-skeleton-starters` 三类模块中的类，必须提供完整行级注释。
- 行级注释要求覆盖：类内核心字段、关键判断分支、主要处理步骤、返回结果与副作用点。
- 禁止仅保留类注释或方法注释而缺失关键代码行说明。
- 对上述模块的新增或修改代码，如不满足行级注释要求，视为不符合交付标准，不允许提交。
- `pom.xml` 注释仅强制要求在 `<dependencies>` 和插件相关配置中编写。
- 依赖与插件注释使用简洁一行描述即可，特殊复杂场景可使用多行注释。
- 版本号属性管理（如 `<properties>`）默认不要求注释。
- 禁止片面注释：若依赖或插件在项目中承担多项核心职责，注释不得只写其中一个次要用途。
