<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.fushuwei.scaskeleton.system.mapper.SysDictMapper">

    <!-- 分页查询字典列表 -->
    <select id="selectDictPage" resultType="io.github.fushuwei.scaskeleton.system.api.response.dict.DictResponse">
        SELECT d.*
        FROM sys_dict d
        WHERE d.is_deleted = 0
        AND d.tenant_id = #{tenantId}
        <if test="request.keyword != null and request.keyword != ''">
            AND (d.name LIKE CONCAT('%', #{request.keyword}, '%')
                 OR d.code LIKE CONCAT('%', #{request.keyword}, '%'))
        </if>
        <if test="request.status != null and request.status != ''">
            AND d.status = #{request.status}
        </if>
        <choose>
            <when test="request.safeSortField() != null and request.safeSortField() != ''">
                ORDER BY ${request.safeSortField()} ${request.safeSortOrder()}
            </when>
            <otherwise>
                ORDER BY d.create_time DESC
            </otherwise>
        </choose>
    </select>

</mapper>
