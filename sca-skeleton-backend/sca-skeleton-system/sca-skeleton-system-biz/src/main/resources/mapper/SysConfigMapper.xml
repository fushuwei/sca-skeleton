<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.fushuwei.scaskeleton.system.mapper.SysConfigMapper">

    <!-- 分页查询系统配置列表 -->
    <select id="selectConfigPage" resultType="io.github.fushuwei.scaskeleton.system.api.response.config.ConfigResponse">
        SELECT c.*
        FROM sys_config c
        WHERE c.is_deleted = 0
        AND c.tenant_id = #{tenantId}
        <if test="request.keyword != null and request.keyword != ''">
            AND (c.name LIKE CONCAT('%', #{request.keyword}, '%')
                 OR c.config_key LIKE CONCAT('%', #{request.keyword}, '%'))
        </if>
        <if test="request.type != null and request.type != ''">
            AND c.type = #{request.type}
        </if>
        <if test="request.status != null and request.status != ''">
            AND c.status = #{request.status}
        </if>
        <choose>
            <when test="request.safeSortField() != null and request.safeSortField() != ''">
                ORDER BY ${request.safeSortField()} ${request.safeSortOrder()}
            </when>
            <otherwise>
                ORDER BY c.create_time DESC
            </otherwise>
        </choose>
    </select>

</mapper>
