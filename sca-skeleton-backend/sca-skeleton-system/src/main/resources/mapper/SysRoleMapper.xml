<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.fushuwei.scaskeleton.system.mapper.SysRoleMapper">

    <select id="selectRolesByUserId" resultType="io.github.fushuwei.scaskeleton.system.entity.SysRole">
        SELECT r.*
        FROM sys_role r
                 INNER JOIN sys_user_role ur
                            ON ur.role_id = r.id
                                AND ur.is_deleted = 0
                                AND ur.user_id = #{userId}
                                AND ur.tenant_id = #{tenantId}
        WHERE r.is_deleted = 0
          AND r.tenant_id = #{tenantId}
        ORDER BY r.sort ASC
    </select>

</mapper>
